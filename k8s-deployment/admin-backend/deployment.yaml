apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-backend
  namespace: food-order
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-backend
  template:
    metadata:
      labels:
        app: admin-backend
    spec:
      containers:
      - name: customer-backend
        image: gimhan764/smsa-1-admin-backend:1.5
        ports:
        - containerPort: 5000
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: postgres-secret
